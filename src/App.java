import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class App {
    public static void main(String[] args) {
        
        // /*Converter uma data para string */

            // LocalDateTime date = LocalDateTime.now();
           
            // DateTimeFormatter formato = DateTimeFormatter.ofPattern("dd/MM/yyyy HH:mm");
           
            // String dateFormatado = date.format(formato);
            // System.out.println(dateFormatado);


        // /*Converter uma string para data */

            // String dataString = "16/01/2024 12:30";
        
            // DateTimeFormatter formato2 = DateTimeFormatter.ofPattern("dd/MM/yyyy HH:mm");
        
            // LocalDateTime dataConvertida = LocalDateTime.parse(dataString, formato2);
            // System.out.println(dataConvertida);


    }
}
